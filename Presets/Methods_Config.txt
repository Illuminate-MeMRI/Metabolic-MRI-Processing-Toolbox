name = Noise Analysis
callback = @memri_noise();
nfo = Analyse noise data and calculate noise-covariance matrices. Frequency domain data is used to generate noise-vectors if noise data is absent, preferred domain is then ignored. Noise import options available, see "Main".
varname = masksize
    query = Noise mask size (an integer, default, or all):
    input = default
    display = default
    type = string    
    uistyle = edit 
varname = independence
    query = Remove co-variance i.e. independent coil channels:
    input = 1, 0
    display = Yes, No
    type = int
    uistyle = popup 
varname = noiseDomain
    query = Preferred noise domain for processing:
    input = 2, 1, 0
    display = Frequency, Time, k-Space
    type = int
    uistyle = popup 

name = Average
callback = @memri_average();
nfo = Calculate the mean of the data over the "aver" dimension.

name = Hamming Correction
callback = @memri_hamming_correction();
nfo = Correct for an imperfect Hamming window used with weighted CSI acquisition(s). 

name = iFFT (k-space)
callback = @memri_ifft_spatial();
nfo = Apply discrete inverse Fourier transform i.e. k-space to (spatial) time domain.

name = FFT
callback = @memri_fft();
nfo = Apply discrete forward Fourier transform i.e. time domain to frequency domain.
varname = correct_N
    query = Correct for 1/#samples in MATLAB's discrete FFT:
    input = 0, 1
    display = No, Yes
    type = int    
    uistyle = popup 
varname = correct_onesided    
    query = Apply half first point correction (i.e. half weighting):
    input = 1, 0
    display = Yes, No
    type = int    
    uistyle = popup
varname = double_shift
    query = Enable double shift for FFT of (symmetric) echoes:
    input = 0, 1
    display = No, Yes
    type = int    
    uistyle = popup

name = PCA Denoising
callback = @memri_pca_denoising();
nfo = Apply a principle component analsyis based denoising method. Preferably applied before coil channels are combined.
varname = patch_size    
    query = Patch-size for voxel convolution window:
    input = 5
    display = 5
    type = int    
    uistyle = edit
varname = do_svd    
    query = Use custom fast SVD-method:
    input = 1, 0       
    display = Yes, No
    uistyle = popup
    type = int

name = WSVD
callback = @memri_wsvd();
nfo = Combine coil channels using whitened singular value decomposition.
varname = identity_matrix
    query = Use identity-matrix as noise-covariance matrix:
    input = 0, 1
    display = No, Yes  
    type = int
    uistyle = popup
varname = reference_channel
    query = Amplitude scaling reference channel:
    display = Maximum amplitude/voxel, Maximum amplitude/volume, First channel   
    input = 1, 2, 0
    type = int   
    uistyle = popup
varname = method
    query = Decorrelation method
    input = 1, 2, 3, 0
    display = Whitening, Cholesky, ZCA, None
    type = int   
    uistyle = popup

name = Roemer
callback = @memri_roemer();
nfo = Combine coil channels by optimizing resulting SNR. A seperate noise measurement usually improves calculated sensitivity maps.
varname = identity_matrix
    query = Use the identity-matrix as the noise-covariance matrix:
    input = 0, 1
    display = No, Yes  
    type = int
    uistyle = popup

name = FFT (k-space)
callback = @memri_fft_spatial();
nfo = Apply discrete forward Fourier transform i.e. (spatial) time domain to k-space.

name = iFFT
callback = @memri_ifft();
nfo = Apply discrete inverse Fourier transform i.e. frequency domain to time domain.
varname = correct_N
    query = Correct for 1/#samples in MATLAB's discrete (i)FFT:
    input = 0, 1
    display = No, Yes
    type = int    
    uistyle = popup 

name = Test
callback = @memri_test_that_doesnt_exist();
nfo = This is a test method to test the method modules in this processing GUI. Lorem Ipsum, Banana. This will crash if you run it ;)
varname = test1
    query = Time to test 1 popup:
    input = 1, 0
    display = Yes, No
    type = int
    uistyle = popup
varname = test2
    query = Time to test 2 edit:
    input = not used for edits :)
    display = Is this showing? Do commas work? A comma after NOW, NO this is invisible to the user.
    type = int
    uistyle = edit
varname = test3
    query = Time to test 3 edit:
    input = 0, 1
    display = No commas do not work
    type = int
    uistyle = edit
varname = test4
    query = Time to test 4 popup:
    input = 0, 1, 2, 3, 4, 5
    display = abra, kadabra, alakazam, sigh, annoying, 6
    type = int
    uistyle = popup
varname = test5
    query = Time to test 5:
    input = 0, 1, 2, 3, 5, 6
    display = Ash, Jigglypuff, Pika, chu, Pichu, Mew
    type = int
    uistyle = popup
varname = test6
    query = Time to test 6:
    input = 0, 1
    display = No, Yes  
    type = int
    uistyle = popup
varname = test7
    query = Time to test 7:
    input = 0, 1
    display = No, Yes  
    type = int
    uistyle = edit
varname = test8
    query = Time to test 8:
    input = 0, 1
    display = No, Yes  
    type = int
    uistyle = popup
