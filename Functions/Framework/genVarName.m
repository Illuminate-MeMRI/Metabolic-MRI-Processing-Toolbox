function vname = genVarName(str)
% Generate valid name for variables - but I dont remember the function
% and matlab does not give it as predictive text while coding...
vname = matlab.lang.makeValidName(str);